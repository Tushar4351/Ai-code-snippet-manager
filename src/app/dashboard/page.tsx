import NavBar from "@/components/HomeComponents/NavBar";
import React from "react";

const page = () => {
  return (
    <div>
      <NavBar />
      dashboard page
    </div>
  );
};

export default page;
