import NavBar from "@/components/HomeComponents/NavBar";
import React from "react";

const page = () => {
  return (
    <div>
      <NavBar />
      dashboard page come back
    </div>
  );
};

export default page;
